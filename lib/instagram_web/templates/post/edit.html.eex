<h1>Edit Post</h1>

<%= render "form.html", Map.merge(assigns, %{action: Routes.post_path(@conn, :update, @post), method: :put}) %>

<span><%= link "Back", to: Routes.post_path(@conn, :index) %></span>
